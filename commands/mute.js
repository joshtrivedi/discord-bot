module.exports = {
    name: "mute",
    description: "mute a user for a given amout of time",
    access: "moderators",
    async execute (msg, args){
     const {
         content, channel, author
     } = msg
     
    }
}