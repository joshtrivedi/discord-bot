const discord = require('discord.js');

module.exports = {
    name: "removeEmoji",
    description: "Removed emoji from server guild",
    access: "moderators",
    async execute(msg, args){
        
    }
}